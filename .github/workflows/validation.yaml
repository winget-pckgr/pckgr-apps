name: Winget PR Test

on:
  pull_request:
    paths:
      - 'manifests/**'

jobs:
  test-winget-install:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run winget test script
      shell: pwsh
      run: |
        $modifiedManifests = git diff --name-only origin/master...HEAD -- 'manifests/**/*.yaml'
        foreach ($manifest in $modifiedManifests) {
            .\scripts\validation.ps1 -ManifestPath $manifest
        }
